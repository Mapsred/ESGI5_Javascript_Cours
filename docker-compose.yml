version: '3'
services:
    node:
        image: 'node:latest'
        volumes:
            - './:/app'
        working_dir: /home/node/app
        ports:
            - '8060:3000'
        command:
            - /bin/sh
            - '-c'
            - 'npm install && npm start'
